## Packages
(none needed)

## Notes
Uses existing installed cmdk for command palette
Assumes backend supports search via ?search= query param for list endpoints (optional)
Routes are read-only (no create/update/delete endpoints in shared/routes.ts) — UI is content-first
